window.default_page_live_time = 1000 * 60 #одна минута * 60 #один час
window.date_time_names = <%= Utils.datetime_names.to_json.html_safe %>

#window.date_class_selector        = ".date"
# class names:
#window.abs_top_class              = "abs-top"

# selectors:
#window.content_selector           = "#content"

# attribute names:
#window.attachments_attr           = "attachments"


# attribute values:
#window.fixed_value                = "fixed"

#tag names:
#window.input_tag                  = "input"

#signatures
#window.backend_sign               = "back"

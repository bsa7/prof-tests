#!/bin/bash
if [ -f env ]
then
  env=`cat env`
else
  env=`cat env_production`
fi
ps -ef | grep sidekiq | grep $env | grep -v grep | awk '{print $2}' | xargs kill -9
RAILS_ENV=$env bundle exec sidekiq -d -C config/sidekiq.yml
